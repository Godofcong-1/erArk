# Plan 22（三期·方案）：生长养成系统 —— 养成事件系统

> 本文件是 Plan 22 三期的**纯方案**。逐文件改动步骤、构建、验证清单、回滚与实施过程记录见
> `plan_22_生长养成系统_三期_实施步骤与记录.md`（下文简称"实施文档"）。
> **总目标、57 条已确认口径、全局现状调查、跨期风险与范围外事项一律以
> `plan_22_生长养成系统_总纲.md`（下文简称"总纲"）为准**；
> **`CHILD_GROWTH` 的字段定义一律以 `plan_22_生长养成系统_一期_方案.md` §4 为准**（下文简称"一期方案"）。
>
> ⚠️ 本文件 §3 的小节编号沿用收敛前单文件计划的原编号（3.7），另新增 3.25、3.26。
> 空号的小节在其他期：3.1~3.5 与 3.9~3.23 在一期方案，3.6 与 3.8 在二期方案，3.20 在四期方案。

- 状态：未实施（v3，2026-09-06；与总纲同步）
- 前置：**一期必须先完成**（事件要有可影响的对象：能力、性格倾向、出勤记录）；**与二期相互独立，可调换顺序**
- 本期覆盖的已确认口径：12、16、25、26、32、33、34、36、44（编号见总纲「已确认口径」）
- 预计改动量：约 **13 个文件**、约 **1200 行**（不含事件条目本身）
- 风险等级：**中低**（新建子系统为主；唯一需要小心的是入队节流与幂等）
- 适用代码快照：`master @ 6aa5090e3`
- 实施前提：先通读总纲 §2、一期方案 §4 与本文件；实施中发现与方案冲突的事实，**先更新本文件再动代码**

---

## 1. 本期目标

取自总纲 §1 的第 9 条，并纳入口径 25、26、36：

1. 全阶段（婴儿→少女）的养成事件由玩家在「处理公务」中决断，选择影响性格倾向与养成走向
2. 事件表支持批量增删改查，新增一条事件只需往 CSV 追加一行
3. 孩子之间的互动（兄弟姐妹、同班同学）通过关系数值 + 养成事件呈现
4. 成年时在大礼堂举行毕业典礼与成年纪念事件

**验收标准**：能在处理公务中逐条处理养成事件，选项影响性格倾向；事件表可批量增删改查；成年时触发毕业典礼。

---

## 2. 本期专属现状调查

全局现状调查见**总纲 §2**，本节只补本期最需要盯住的三处。

### 2.1 既有事件系统的形态

`data/event/event.json`，每条含 `uid / adv_id / behavior_id / start / type / text / premise / effect`，由 `Script/Design/event.py` 处理，**按 `behavior_id` + `premise` 触发**。ArkEditor 已支持事件的增删改查。

⚠️ **触发模型不同**：既有事件是"做了某个行为时触发"，养成事件是"孩子的成长状态满足条件时，按周期/随机入队"。所以本期复用它的**数据形态与编译链**，但**另起一套触发逻辑**（§3.7）。

### 2.2 处理公务的入口

指令 2001 `official_work`（`InstructConfig.csv:60`，`Script/Core/constant/Behavior.py:140 OFFICIAL_WORK`），前提 `IN_DR_OFFICE｜HAVE_OFFICE_WORK_NEED_TO_DO`，效果在 `constant_effect.py:433` 按学识与办公室等级消耗工作量。

养成事件挂在这条指令上：玩家在博士办公室处理公务时，队列里的养成事件逐条弹出决断。

### 2.3 事件幂等的既有做法

一期已在 `CHILD_GROWTH` 里建好 `event_history`（键 str 事件 uid，值 dict 含 `time` 与 `choice`），本期开始写入。⚠️ 一次性事件必须查 `event_history` 防重复触发——这是本期唯一的幂等要求，成长阶段跃迁类事件靠素质变化天然幂等（如成年事件的守卫是素质 103→104），但"第一次上课""第一次考砸"这类必须靠 uid 记录。

---

## 3. 设计决策

### 3.7 养成事件独立子系统，归入处理公务

**载体**：复用既有事件系统的数据形态（`data/event/event.json` 的 uid / premise / effect 结构），但**独立成一套养成事件配置**。理由见 §2.1：既有事件按 `behavior_id` 触发，养成事件按"孩子的成长状态 + 随机/周期"触发，触发模型不同，硬塞进去会污染既有事件的判定路径。

**数据流**（与既有事件一致，可复用 ArkEditor 与 buildconfig 链）：

```
data/growth_event/*.csv  →  buildconfig  →  data/Growth_Event.json  →  运行时 config
```

**触发与呈现**：

1. 每日结算时按前提筛出可触发的养成事件，写入待处理队列
2. 玩家在博士办公室「处理公务」（指令 2001）时，队列中的养成事件逐条弹出
3. 每条事件给 2~4 个选项，玩家决断
4. 选项的 effect 影响：性格倾向（`personality_point`）、能力经验、好感信赖、后续事件解锁

**入队节流**（口径 32）：每日最多入队 **1~2 条**，未处理的留在队列到下次公务。⚠️ 这条不是可选优化——多孩时若不节流，一次公务会涌出十几条事件，玩家会直接失去判断意愿。

**结果尺度**（口径 33）：选错会导致性格偏向不如预期、好感小幅下降，但**不掉能力、不造成不可逆损失**。养成事件是塑造方向的，不是惩罚失误的。

**选项前提**（口径 34）：复用既有前提系统与 CVP token，不满足时**置灰并标明原因**（如"好感不足"），而不是隐藏——让玩家看见"这里本来有更好的选择，但我没养到"。

**批量增删改查**（口径 12、16）：事件表按阶段分文件（`婴儿.csv` / `幼女.csv` / `萝莉.csv` / `通用.csv`），每行一条事件，列含 `cid / 阶段 / 前提 / 文本 / 选项1~4 / 各选项effect`。批量生成时只需往 CSV 追加行，与既有口上 CSV 的工作方式一致。

⚠️ ArkEditor 侧能否扩展出"养成事件"分类，需在实施前确认（总纲 §6-5）。若不能扩展，退路是直接编辑 CSV——事件表是扁平的一行一事件结构，手写可行，只是不如编辑器方便。

### 3.25 孩子之间的互动：关系数值 + 养成事件

口径 25、36 的落地方式：

| 层 | 做法 |
| --- | --- |
| 数据 | 复用既有 `relationship` 结构（`game_type.py:366~370` 的 `father_id` / `mother_id` / `child_id_list`）建立兄弟姐妹关系；同班同学关系由课表反查（同一格子的学生互为同学） |
| 呈现 | 全部通过养成事件呈现——"薇薇安和妹妹为了一个玩具吵架了""两个孩子在实践课上互相帮忙" |
| 结算 | 事件选项影响两个孩子的好感与各自的性格倾向 |

⚠️ **不做 NPC 之间的自主行为目标链**（口径 36）。让两个孩子在场景里自主互动需要给 NPC AI 加一整套"以另一个 NPC 为目标"的行为选择，影响面远超本计划。用事件呈现，成本低且叙事密度更高。

### 3.26 毕业典礼与成年纪念：做成事件，不做指令

口径 26、44。

| 事件 | 触发条件 | 舞台 |
| --- | --- | --- |
| 毕业典礼 | 成年结算时（素质 103→104，二期方案 §3.8 的 `check_grow_to_girl`） | 大礼堂（一期方案 §3.10 新建的 `Auditorium`） |
| 成年纪念 | 毕业典礼之后 | 同上 |

⚠️ **不做成指令行为**（口径 44）：毕业典礼是一次性的叙事节点，做成玩家指令意味着要配行为、时长、口上、前提一整套，而它一辈子只触发一次。事件系统的一次性叙事正是为此而生。

⚠️ 大礼堂在一期只做公开课（口径 15），**它的养成事件舞台功能在本期才启用**——这是一期与三期的接口。

---

## 4. 数据结构设计

本期**不新增角色字段**——`CHILD_GROWTH.event_history` 与 `personality_point` 已在一期建好并做了存档回填，定义见**一期方案 §4.1**。

本期新增的是全局待处理队列：

```python
        self.growth_event_queue: list = []
        """ 待玩家在公务中处理的养成事件队列 元素dict:{"uid": 事件uid str, "chara_id": 孩子角色id int, "add_time": datetime}
            每日最多入队1~2条（口径32）；处理后出队并写入该孩子的 event_history """
```

挂 `Rhodes_Island`（`game_type.py:1195`），旧档在 `save_handle.py:551` 附近回填为 `[]`。

⚠️ **这是本期唯一的存档结构改动**。一期没有预建它，因为队列的元素结构直到本期设计定型才确定；它是 list 不是 dict，回填只需 `hasattr` + `= []`，风险很低。

### 4.1 养成事件表的列结构

`data/growth_event/{婴儿,幼女,萝莉,通用}.csv`：

| 列 | 类型 | 说明 |
| --- | --- | --- |
| `cid` | str | 事件 uid，文件名自动加前缀（照口上 CSV 的 `buildconfig.py:189~193` 机制） |
| `stage` | int | 适用阶段素质 id（101 婴儿 / 102 幼女 / 103 萝莉 / 0 通用） |
| `once` | int | 是否一次性（1 则查 `event_history` 防重复） |
| `weight` | int | 入队权重 |
| `premise` | str | 触发前提，`&` 连接，复用既有前提与 CVP token |
| `text` | str | 事件正文 |
| `option_1~4` | str | 选项文本，空则该选项不存在 |
| `option_1~4_premise` | str | 选项前提，不满足则置灰并显示原因 |
| `option_1~4_effect` | str | 选项结算，复用 CVE token 形式 |

---

## 5. 界面与信息架构

### 5.1 公务中的事件弹出

在既有的处理公务流程里插入养成事件段：

```
【养成事件】薇薇安 · 萝莉期第 38 天
--------------------------------------------------
  薇薇安拿着这学期的成绩单站在你面前，学识拿了个 C，
  但战斗课的成绩几乎垫底。她低着头，手指绞着衣角。

  1. 摸摸头，说"尽力就好"        （亲密 +，倾向：脆弱）
  2. 指出她战斗课没认真上        （好感 -，倾向：坚强）
  3. 提出陪她一起练              （亲密 ++，好感 +，需好感 ≥ 200）
  4. [置灰] 让她自己想办法        （需信赖 ≥ 50%，当前 32%）
--------------------------------------------------
```

⚠️ **选项的后果提示要写出来**（"倾向：坚强"），但**不写具体数值**。玩家需要知道选择的方向，不需要知道加了几点——后者会让养成变成算数题。

### 5.2 养成事件履历

在一期的养成总览面板（一期方案 §5.4）里加一栏，列出该孩子已触发的事件与当时的选择，读 `event_history`。

---

## 6. 与其他期的接口

| 接口 | 依赖方向 | 说明 |
| --- | --- | --- |
| 一期的 `CHILD_GROWTH.event_history` | 本期写入 | 一期已建字段并回填 |
| 一期的 `personality_point` | 本期写入 | 二期的成年结算读它做选边 |
| 一期的 `attend_class_count` / `absent_count` | 本期读作事件前提 | "最近总是缺课"类事件 |
| 一期的大礼堂（`Auditorium`） | 本期启用其舞台功能 | 一期只做公开课（口径 15） |
| 二期的成年结算 | 本期在其上挂毕业典礼事件 | ⚠️ **若二期未完成，毕业典礼事件挂不上**——此时本期可先做前三项目标，毕业典礼留到二期完成后补 |

⚠️ **三期与二期相互独立，可调换顺序**，唯一的例外是毕业典礼事件依赖二期的成年结算入口（总纲 §3）。

---

## 7. 风险与注意事项（本期）

全计划的风险清单见总纲 §4.1，本节只列三期涉及的项。

| # | 检查项 | 依据位置 | 风险与对策 |
| --- | --- | --- | --- |
| 1 | **入队节流** | 口径 32 | ⚠️ 不节流则多孩时一次公务涌出十几条，玩家失去判断意愿。→ 每日最多入队 1~2 条，未处理的留在队列 |
| 2 | **一次性事件的幂等** | `event_history`（一期方案 §4.1） | 同一事件反复触发。→ `once == 1` 的事件入队前查 `event_history`；出队时写入 |
| 3 | 存档兼容 | `save_handle.py:551` 附近 | 新增 `growth_event_queue` 一处。→ `hasattr` 回填为 `[]` |
| 4 | 队列里的孩子已不存在 | 孩子成年 / 存档跨版本 | 出队时角色 id 查不到 → KeyError。→ 弹出前校验 `chara_id in cache.character_data`，无效则静默丢弃 |
| 5 | 选项前提不满足 | 口径 34 | → 置灰并显示原因，不隐藏、不报错 |
| 6 | Tk/Web 双模式 | `web_draw_adapter.py` | 事件弹出是多选项交互。→ 只用 `Script/UI/Moudle/draw.py` 抽象类；照既有事件的绘制方式 |
| 7 | 编译链 | `buildconfig.py` | 新增 CSV → JSON 分支写错会让整个构建失败。→ 照既有事件与口上的处理分支写，先跑通空表 |
| 8 | ArkEditor 可扩展性 | 总纲 §6-5 | ⚠️ 实施前先确认。→ 不能扩展则退回手写 CSV，不阻塞本期 |
| 9 | 多周目 | `new_round.py:37~49` | 队列随 `Rhodes_Island` 重置；孩子本身新周目不存在 |
| 10 | 循环导入 | `Rhodes_Island` 在 Core，事件逻辑在 System | → 数据在 Core、逻辑在 System，函数内延迟 import |

**本期不涉及**：地图、时停、催眠、`BODY_H_STATE`、NPC AI 目标链（口径 36 明确不做）。

---

## 8. 不在本方案范围

- 课表、六种课型、上课结算、`<课>` 标识 → 一期
- 日程模板、幼女跟随母亲、成年结算本体 → 二期
- 妊娠期胎教、婴儿期照料行为差异化、性技实操课 → 四期
- **NPC 之间的自主互动行为链**（口径 36）：孩子间互动只走关系数值 + 事件呈现
- 总纲 §5 列出的全计划范围外事项

---

## 9. 第二轮追加调整

（实施完成后按 §9.1 / §9.2 顺延追加，只写设计；对应的实施记录写在实施文档 §6.5）
