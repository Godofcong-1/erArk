# 生长养成系统回归测试

覆盖 `Script/System/Education_System/` 及其全部外部挂接点（结算器、状态机、前提、面板、存档、口上与事件数据）的无头回归套件。系统说明见 `.github/prompts/数据处理工作流/生长养成系统.md` §16。

## 怎么跑

```bash
.conda\python.exe tools/tests/education/run_all.py            # 全部 15 个文件、1770 条断言（有真实存档时；没有时两段跳过，见 fixture 陷阱），约 1 分钟
.conda\python.exe tools/tests/education/run_all.py semester   # 只跑文件名含关键字的
.conda\python.exe -u tools/tests/education/test_semester.py   # 单跑一个（进程末尾会 os._exit）
```

- **必须用 `./.conda/python.exe`**，裸 `python` 是商店空壳。
- 每个文件各起一个进程：游戏的全局缓存只能初始化一次，同进程串跑会互相污染。单文件超时 240 秒。
- 日志写在 `%TEMP%/erArk_education_tests/<文件名>.log`；汇总行里的 `PASS= / FAIL=` 来自各文件末尾的同名行。
- 改了 CSV（口上 / 前提 / 效果 / 事件）后先删 `data/Character_Talk.json` 再跑一次 `buildconfig.py`，然后 `git checkout -- data/po/`（构建会清空 PO），否则断言读到的是旧生成物。

## 文件与覆盖

| 文件 | 覆盖 |
| --- | --- |
| `_bootstrap.py` | 公共引导：初始化链、屏蔽绘制并记录文本（`drawn_text`）、`askfor_all` 桩、`check / section / finish`、`make_character` fixture、`set_time / move_to / open_all_classroom / clear_schedules`；未捕获异常时 `os._exit(2)`（否则非守护线程会让崩溃的进程挂住） |
| `test_schedule.py` | 教室列表与开放、格子读写、临时课覆盖层（含 `include_temp=False`、一键选课不选临时课）、教师反查与冲突、个人课表、`get_now_course / get_now_teaching`、上课地点、实习导师；Plan 26：撞课判定照每周课表（被临时课顶掉的教师照样判撞课）、`get_now_teaching(0)` 只在玩家就在那间教室时返回临时课、上课地点过滤未开放的场所（`judge_scene_open`）、`judge_course_need_pass`、`get_upcoming_course` 不再收 `now_time`；Plan 27：全局课表那一格清空即视为没课（`get_course_at` 为 None），格子在、教师为 -1 仍是一节课，临时课覆盖层给出的格子（预约 / 必修）照算；Plan 28：个人式课这一节上不成（少女的过家家、未解锁的游泳池）视为没课（`judge_personal_course_valid`、`get_course_at` 为 None），实习课无人在岗仍是课、必修覆盖不受影响，非法目标的兴趣课同样视为没课；Plan 29：兴趣课读书借不到书视为没课（`judge_interest_course_is_read_book` / `judge_personal_course_valid`：书库有书、全被借走、手上有书、只剩读完的、只剩读不了的、判定不写数据）；Plan 31：`get_course_at` 只对学生岗生效（改岗后本节 / 下一节 / 个人式课 / 必修覆盖都为 None，个人课表原样保留，改回恢复）、教师反查只认教师岗（`judge_schedule_teacher`：前教师改当学生后 `get_now_teaching` / `get_upcoming_teaching` 为 None，`get_teacher_cell` 与撞课判定照旧，玩家不看岗位）、`get_now_course` 的返回说明；Plan 32：教师候选排除被监禁的教师，一键排课不再把她排进空格子（L21） |
| `test_auto_schedule.py` | 一键排课的幂等 / 可复现 / 无教师冲突 / 周末不排 / 实践教室科目约束；一键选课 |
| `test_class_ai.py` | Plan 24 起师生走工作链，派发一律跑整条 `find_character_target`（`dispatch(cid, now_time)`）：翘课概率表与定种子掷骰（同节稳定、0/1 边界、每节一掷）、教师可用性（含换岗 / 跟随 / 睡着 / 不在岛上 / 临盆 / 产后 / 助理 / 体检中；住院表撞号照常可用，Plan 32 M4）、缺课去重、状态函数 `get_teacher_duty` / `get_course_stage`（六种状态、必修豁免、非本岗恒为 NONE、前提只读不惰性创建养成数据）、教室课与个人式课的整链派发（715 移动、304 / 713 / 716、教室或地点解析不出交回娱乐链）、兴趣课逐项（`class_ok` 的全部 16 项：715 → 716 执行该娱乐的行为、截到本节结束）、实习课逐岗（面板可选的全部岗位：715 → 716 `intern_class`；多房间岗位的导师在哪间学徒就去哪间）、两道闸（721 记缺课、714 翘课）、必修实操课豁免与必修生覆盖、教师按课表走班（实践教室、被临时课顶掉、办公室待命、午休、到岗时间、周日有课照上）、学生到岗时间先去第一节课、预到岗（已在实操教室原地等开课）、normal 门槛（跟随不再来回走，临盆 / 产后 / 助理 / 体检中的教师不授课、学生自习）、外层闸（图书馆管理员与厨师的派发不变）、口径收窄（改岗女儿不上课、改回恢复）、见学让路与回落链、萝莉按日程见学、没课节次的去向；Plan 25：学生截短规则（A 待赴实操课 / B 马上开课；`begin()` 造「行为已进行一段、玩家这一步已跨过开课」的情形，断言的是截出来的时长）、开课后到场加入课堂（722）、醉酒教师不可用、303 拉人过闸（`teach_pull()`）、561 只认学生岗、成年必修生；Plan 26：未解锁场所与兴趣课条件不符都交回娱乐链、待赴的实操课被玩家提前开讲且人已在教室 → 722、课堂模式下受邀到场走 515 → 722（非学生干员到场即收场、不进 H）而普通群交走 505 → 96、母亲睡着时见学回落自由玩耍（公务事件的 `self_mother_available` 不受影响）；Plan 27：已停课 → NONE、交回娱乐链且不记出勤、不为它提前动身，必修覆盖到空格子与预约在空格子上的实操课照旧，561 的回落只挑已开放的理论教室（定种子），爆睡（行为是睡觉、没挂标记）的学生不被 303 拉；Plan 28：个人式课上不成 → NONE，体力低 / 心情闸必中都不派 721 / 714、不记缺课不挂翘课 flag，不为它提前动身，截短规则 B 不截（对照：萝莉照截），幼女的体育课排在未解锁的游泳池照常默认见学，条件恢复 / 解锁后照常上课；Plan 29：体育 / 兴趣课派出 716 即记一节出勤（同一节再派不重复、逐项 16 种兴趣课都记、实习课 716 不记由 552 记），兴趣课读书先借书并写入 `book_id` / 书名、手上有书不另借、借不到书 → NONE 交回娱乐链且不记出勤、判定与执行之间被借走则 716 等 1 分钟，401 的借书与借不到对照；Plan 30：714 在教室里先离开那一步不记、开始摸鱼记一节缺课与翘课数、同一节不重复、flag 挂着的后续节次各记一节、同一节体力缺课后再翘只记一次（翘课数不加），721 后同一节 716 照派不计出勤、下一节照常，623 记被抓日期、被抓当天 `get_course_stage` 不再给 SKIP、303 拉人不判翘课、次日照常；Plan 31：M3 教师可用性（空气催眠只在人已在本节教室时能到岗、木头人一律来不了、木头人时学生派 713）、`judge_skip_class_today`（今天挂上的才算，前一天的 flag 照常掷骰可拉，离岛两天回岛派 715）、翘课日的两处口径（13:45 判 NONE 不派 715 / 720、娱乐截到开课那一刻、开课那一刻 714 记缺课、空节 120 分钟电影截到 10:30）、303 不拉时间线在开讲之后的学生、拉人过听课行的 normal 门槛（跟随 / 有需求的不拉，空气催眠的照拉）、H1 行为循环（玩家一步 60 分钟、教师换两跳晚到：两种处理顺序下第 2 节在学生坐下时结算一次、第 3 节不重复，每步 10 分钟的对照；只让 101 / 201 参加、体力气力 2000、每步清需求）；Plan 32：L29 `CLASS_SM_SET` 与 target 组 07 / 08 的实际状态机一致，M4 住院表的病人编号撞上教师 / 母亲的角色 id 时照常可用，M5 改岗萝莉只在自己的休息时间见学（工作日白天派工作链、晚上照旧见学、学生岗照旧），L3 人已在上课地点也截到开课那一刻，L4 截短排在实时结算之前（真实行为循环：一步 60 分钟只结算 60 分钟），L7 同一节已记出勤时 721 / 714 不再记缺课，L10 临时实操课上不够格的选修生开课前后都判教师来不了、降级自习；截短规则的断言改调 `judge_student_leave_truncate`；Plan 32 实施复审补：M4 学生侧（撞号时派 304）、L10 跑完自习（548 记出勤与自习档经验）、H1 走真实玩家分支与行为循环（幽灵课堂在玩家这一步收掉、循环收敛、不派 722；把 `settle_orphan_class` 桩空的对照撞护栏）；Plan 32 §8.1：课堂 H 里已有别的学生在 H 中时，晚到的学生照常 722 加入、够不上课堂的 713 自习、提前开讲的待赴必修生照常加入，都不再先命中目击 H（target 500）；非学生照旧目击（状态机 40 桩成空函数，只看派发） |
| `test_growth.py` | 速度曲线、教育区加成、成长停滞减半、学生 / 教师 / 见学结算、同一节去重与 `count_attend`、成年结算、养成数值读写、两份候选名单、待炫耀只记科目与幼女 / 萝莉期的女儿（跑真实的 `gain_ability`）；Plan 26：性技科目的课堂只发理论经验（`get_class_exp_id` 与映射表的类型 / 覆盖面守卫、腰技只给习得、师生两侧）、未成年一门性技都没学会时不凭珠升技巧（面板说明、学会一门后可升、成年不受影响）；Plan 28：婴儿不在 `npc_id_got`，学生岗也不进个人课表名单；Plan 29：`get_stage_start_day` / `get_stage_day` 各阶段起点、下限夹 0、成年为 0，阶段进度改用它后数值不变；Plan 30：养成数值 24（没有养成数据为 0）、`judge_absent_this_period`（同一节任一时刻 / 下一节 / 节次外 / 别的日期、不惰性创建）、`judge_selected_cell_real`（已停课不算、没排教师仍算、改岗不算、场地未开放不算、条件不符不算、书库借空仍算）；Plan 31：养成数值 25（累计实操课次数）、`judge_have_course_type`（改岗、已停课、课型不符都为 False）；Plan 32：M1 `game_time.count_play_day`、阶段进度按可游玩天（全年 122 个季月出生日期用真实时钟逐日推进，婴儿中期窗口都开得出来）、9/7 出生的婴儿跨过季月交替只走一天，M3 成年后改写性格倾向即重选这一对素质（倾向归零两侧不动、未成年不选边），L27 / L28 基础值按课型常量取与 557 的说明；改岗段换回学生岗（L20 之后待炫耀只记学生岗）；Plan 32 实施复审补：`count_play_day` 起点落在被跳过的月份、萝莉期 122 个出生日期都有 ≥ 70 的可游玩日、婴儿期 ≥ 50 严格早于 ≥ 60、成长加速药分支（M1），M3 只重选被改写的那一对 |
| `test_semester.py` | 学期时间、本学期出勤、四档判据、等级增量、学期切换幂等、成绩单历史上限与正文；Plan 29：`settle_course_attend` 的守卫、去重与节次外不记，只排个人式课的孩子按档评（不再无课可评、缺一节按实际算），改岗萝莉照出成绩单并带不全的理由（写明改任的岗位 / 岗位为无、正文与学期切换冻结的快照都有、学生岗为空、旧快照无 `reason` 键）；Plan 30：学期中途回到学生岗写理由，成绩单的缺课含翘课（上 1 翘 3 → 25%、待努力，节次外不记），学期基线记岗位，改岗理由九种组合（学期初学生岗 / 非学生岗 / 未知 × 结算时 × 有无记录），Q1：成年女儿第一次记账照立基线、整学期不在学生岗不出成绩单（旧档学期初未知同样不出）、成年学生岗照出、萝莉整学期不在学生岗照出并写「担任…没有上课」、学期中途离开照出最后一份，`judge_adult_out_of_school` / `judge_report_card_checkable`（有待查看时可用、看完不可用） |
| `test_schedule_template.py` | 预设模板、增删改、套用与覆盖、need 校验（幼女 / 萝莉 / 少女 / 学生岗）、`judge_require` 的 `/` 或与 `W` 岗位语法、选活动三组与「限幼女/萝莉」标注、条件不符退回自由选择、有课的时段也照写（课表在节次级别优先）、每日改写、幼女默认池（过家家 / 自由玩耍随机）；Plan 27：`judge_activity_place_open`、地点未开放的时段退回自由选择与「未开放→自由选择」标注、幼女默认池滤掉未开放的（游戏室没解锁只剩自由玩耍，解锁后恢复）；Plan 28：`judge_activity_schedulable`、候选没有照料卵、旧档残留的照料卵跳过并标「条件不符→自由选择」、持卵钩子换上的照料卵时段不被改写冲掉（打桩 `egg_handle.is_egg_layer / have_need_tend_eggs`）；Plan 32：L17 删模板遍历全部角色（离线的女儿一并解开、「套用中」也数离线的人），L18 成年非女儿干员离开学生岗后当学生时套的日程不再改写，L28 模板键与 need 分隔符集中在 `education_constant` |
| `test_sex_class.py` | 临时课存取与过期清理、星期→日期（含季月月底按游戏时钟跳到下个季月）、谁能参加（前置修习与必修豁免）、三次提醒的跨越判定、开课 / 下课三档、下课后 ended 与当场开的课即删、主修加成、旁观名单与结算、出勤只给女儿与学生岗；Plan 26：提前开预约课（复用预约不另开、`find_reserved_class` / `find_class_to_start`、按教室取必修名单、按预约时刻判档、下课后 ended 与同节重开）、在别的教室开课挪用预约、只收学生岗、提醒文案；Plan 27：同一节下课后重开，到场学生再记一节（L2 按设计保留，锁定断言）；Plan 28：选修名单只收学生岗；Plan 30：本节已记缺课时开课（传 `cache.game_time`）与 722 晚到都不记出勤、下一节照常；Plan 31：实操课门槛只判不扣（心控中的成年学生，门槛、前提与入课判定各求值多次，理智与今日消耗不变、理智为 0 时不解除催眠；缺省调用照旧扣理智、照旧解除），提前开讲按那一节判缺课（提前 5 分钟开讲、722 晚到、这一节本身缺过课、节次外当场课），累计实操课次数随出勤 +1；Plan 32：H1 课堂 H 以别的方式结束时一并下课（课上不显示 6008、371 / 373 / 375 / 362 各效果串的组成、玩家这一步走完即下课、转单人 H 课照上、结束 H 后下课、旧档幽灵课玩家下一步或跨天收掉、最后一名学生力竭退出走真实调用链），L7 记出勤时写「这一节已结算」标记（按时 / 提前 5 分钟开讲、下一节照记缺课），L8 同节重开时开课前的必修名单与开课条目同源，L9 课堂成员按身份认、旁观名单另过状态（时停），L11 课堂模式的模板选人只列课堂成员；Plan 32 实施复审补：L11 主修加成只给课堂成员（真实的 464 + 10014 摆现场：成员乘加成，被 464 拉进 H 的成年干员、别处 H 里的人不乘） |
| `test_prenatal_baby.py` | 胎教累积 / 出生折习得珠（Plan 26：不写任何经验、双胎各自全额、按比例折算、折出 0 珠时的文本）/ 读口、效果 555、六个照料行为的效果链与 CVE 实跑、喂奶 556；Plan 32：真实的 `_settle_baby_grow_up` 当天娱乐在幼女默认池、婴儿期入队的婴儿桶事件清掉（L19 / L16），真实的跨天结算只清过期的翘课 flag、今天过生日的女儿队首是生日事件（L5 / M2） |
| `test_growth_event.py` | 候选人（成年少女不进名单与容量）、同胞与同学、事件桶与互动对象、每日派发、注册式容量与抬头、毕业 / 期末入队（毕业典礼不受队列上限）；Plan 26：同胞只取幼女 / 萝莉、17 条通用事件对婴儿不成立、成年女儿不推期末事件、期末 13 的档位前提；Plan 28：婴儿不在 `npc_id_got` 也进派发名单与容量、必派时派得到、离线的幼女不进、婴儿期抬头，走真实出生路径（`born_new_character`）的婴儿逐条逐选项结算婴儿事件不报错；Plan 29：抬头写本阶段第几天（萝莉 300 天 → 第 31 天、幼女 120 天 → 第 31 天、婴儿 5 天 → 第 6 天）、改岗萝莉照推期末事件（用户拍板）；Plan 30：同学与 `self_have_any_course` 只算学生岗的真课（改岗、停课都不算，改回恢复），改岗萝莉抽不到上课 / 同学事件，待努力的萝莉候选里有期末 9 / 10、幼女只过得了 9，萝莉 2 按累计翘课数（只体力缺课的判不过）；Plan 31：同学只取幼女 / 萝莉（成年姐姐不算）、成年结算推通用 59 / 60 到队尾（空队列、已有日常事件、不重复推、已经历的不推、队列已满照样追加），点名课型的事件按课型进候选（萝莉 7 / 8 / 9 / 16、幼女 1 / 22），写成绩单的事件要有待查看的新成绩单且不是档位 3（萝莉 1 / 20 / 26；Plan 32 起挪进期末桶，改验期末 17 / 18 / 19 不在日常候选里），萝莉 6 读本学期，生日 / 季月交替 / 断奶的事件只在正文写的时点；Plan 32：M2 生日当天跨天结算把通用 3 插到队首（不重复、队列已满照插、已经历的不推、不是生日不推、排在日常派发之前），L16 长大时清掉对不上新阶段的事件（阶段桶按 sub_key 与前提里的阶段素质记号、期末桶只按记号、成年桶不动、事件表里的记号全收在 `STAGE_TALENT_PREMISE_SET`；真实的成长结算在换完素质后清，成年时毕业典礼照在队首），Plan 31 L10 段改为萝莉 1 / 20 / 26 已挪进期末桶、L13 段的通用 15 改用幼女；Plan 32 实施复审补：M2 双胞胎按 id 排队首、她或博士被塞着口球照样推入，`judge_premise_all_pass` 的六种情形（空前提、主体不存在、逐条判、跳过 `Weight|0` 与 `high_`、交互对象临时指向互动对象并还原）；Plan 32 §8.2：公务事件的选项按钮不写后果提示，`draw_result` 用一个 WaitDraw 写出选了什么与后果，处理公务的真实流程先结算、记履历再弹结果，置灰选项照旧写原因（WaitDraw 换成记录桩）；2026-09-17：选项顺序每次随机（连开 60 次出现多种排列、每次不重不漏、选中画面第一个按钮拿回的是 CSV 原序号、`option_list` 仍是原序），抬头只占一行（另加载一份干净的 `draw.py`、把 `io_init.era_print` 换成收集器量总宽：`count_title_width=True` 时正好 190、抬头比宽度还长时只画抬头，不传时仍是 190 + 标题宽的旧口径，其他面板不受影响） |
| `test_settle_effects.py` | 512（只发自己的学生、同一节不重复、玩家手动授课只在课表排在这间教室时计出勤）/ 557 晚到补结算 / 548（日程自习不计出勤）/ 549 / 550 / 552 / 553 / 554 / 10014（开课整条链路只记一节出勤）/ 10015 与二段 622 / 623；Plan 26：性技科目的 512 / 548 只发理论经验、不补记初体验，玩家手动授课只在那节临时课的教室里才按性技讲；同一节里连验多种结算时用 `reset_mark()` 清去重标记；Plan 27：550 有待查看才发冻结那份并清 flag、看过之后给截至目前，养成数值 23，玩家手动授课拉来的学生截到本节下课（节次外 45 分钟）、爆睡的学生不拉（`handle_teach` 打桩 `game_update_flow`）；Plan 28：548 在上不成的个人式课（少女的过家家）里只给收益不计出勤（对照：萝莉照计）；Plan 29：552 记的实习课出勤，同一节 `settle_course_attend` 不再记一份；Plan 30：本节已记缺课时 512 / 557 照给收益、不计出勤，下一节照常，623 记下被抓的日期；Plan 31：557 在教师判能到岗时就结算（教师还在路上、随后 512 不重复；教师 H 中、空气催眠且不在本节教室、木头人都不结算；临时实操课与学生不在本节教室不走 557），549 按行为开始那天记 `skip_class_day`，前一天的 flag 不触发 623；Plan 32：L6 512 只发给听课节次与教师开讲节次相同的学生（她下一节是玩家的临时实操课、已坐在这间教室等，上一节的教师晚到开讲），557 的模块说明；Plan 32 实施复审补：L6 被跳过的学生体力不变（走真实的 `judge_character_status`）、L13 走真实 `handle_teach` 的 512（结算学生那一刻科目回落学识、课型按教室） |
| `test_premise_tokens.py` | 前提注册（含 Plan 24 的 18 个师生前提、Plan 25 的 2 个新前提，死前提已删）、师生前提只认本岗、岗位与女儿前提、CVP `Course / CourseType / CourseShowOff / Growth`、CVE Growth、实操课 12 条前提、带教前提、授课指令的前提只看所在场景且三类教室都算、`t_baby_0`（Plan 26：1036 对婴儿不成立）；Plan 27：`Growth|23` 成绩单待查看、授课指令的前提对爆睡的学生不成立；Plan 28：CVP `CourseType` 与学生前提对上不成的个人式课不成立，日程自习（节次内外）的 `<课>` 悬停写「自习中」、听课仍写「上课中」；Plan 30：`self_have_any_course` 只看学生岗的真课（已停课、改岗不成立）、`Growth|24`、`target_report_card_checkable` 注册并挂在 1036 前提串末尾，1036 对成年后不在学生岗的女儿不成立（有待查看的成绩单时成立）、成年学生岗的女儿与改岗萝莉成立、非女儿的学生岗不成立；Plan 31：六个新前提注册、五个课型前提（课表为空 / 只排理论课 / 五种各一格 / 停课 / 改岗 / 只读不惰性创建）、`self_birthday_today`（当天 / 次日 / 2 月 29 日平闰年 / 缺省出生日）、教师能到岗的两个学生前提传教室、改任实习岗不亮 `<课>`、`<翘>` 只认今天挂上的 flag、前教师改当学生 CVP 读自己的课表、玩家手动授课的 CVP 回落（三类教室、节次外、临时课的教室里教师反查优先、没在授课与 NPC 不回落）、同学前提的 docstring；Plan 32：L26 公开课前提，L13 被玩家「授课」拉来听课的学生 CVP 课型按教室判、科目回落学识，L2 今天翘过课的学生在 H 中 / 实操课上 / 正要去上实操课时不判「翘课被抓」，L20 待炫耀只记学生岗（跑真实的 `gain_ability`） |
| `test_panels.py` | 页签容器、全局课表（格子编辑、临时课编辑、必修名单含成年学生岗）、个人课表（首屏只有顶在行首的「选择学生」、走通用 NPC 选择面板的脚本化选人、选课、复制、清空）、养成总览（选人、阶段行的母亲 / 剩余天数 / 预计日期 / 成长停滞、成绩单翻页、待处理）、日程模板、`student_select` 组件；Plan 26：覆盖格「[临]」与选科目页的说明和清空按钮、排实操课页的跨教室提示、个人课表的「（未开放）」「（条件不符）」、课堂模式的邀请名单（打桩通用 NPC 选择面板取名单）；Plan 27：选择活动的「未开放」标注（按钮不超过每格 31 列）、周日实习课只画一句提示；Plan 28：个人课表格子的「/条件不符」「/未开放」（全部兴趣课与体育场地都放得进一格）、必修名单的「*」与明细（体育课标、已停课不标、上不成的不标）、待处理写学期名（没有成绩单时写「新的」）、选择活动没有照料卵；Plan 30：书库借空时排着读书兴趣课的学生照标「*」；Plan 31：待处理的翘课标记只认今天挂上的 flag，排实操课页的选修人数按实操课门槛分出「此刻进不了课堂」一行（实行值不足、监禁，一个都进不了时改写第一行），离岗 / 离岛教师的标注（周表格子写短标注、灰字、悬停写全称，全部科目 × 四字名放得进一格，长名字先截名字、标注照写，个人课表选课页与「每周课表这一格」同样标注）；Web 适配器冒烟；Plan 32：L21 被监禁的教师标「（被监禁）」、灰字、选教师页不再列她，L12 排实操课页的三份选修名单去掉点名必修的人（被监禁的必修生列进「此刻进不了课堂」一行），`<课>` / `<翘>`（L15 自习悬停、L13 听玩家手动授课、L14 缺课休息与在上课地点的 H 里不亮、L1 翘课只在 SKIP 时亮），L28 三个面板删掉不用的导入、周日按一周天数推 |
| `test_talk_data.py` | 教育相关口上的覆盖、行为与前提 token 逐条校验、正文卫生、`tools/official_event_check.py`、配置一致性（含全部指令的 cid 落在自己类型的段内、养成相关指令编号）、ArkEditor 表同步；Plan 30：期末 9 / 10 不再读本学期出勤率，校验工具对期末桶读养成数值 4 / 5 / 6 的主前提与选项前提各报一条（构造违规行调 `check_row`）；Plan 31：炫耀口上按女儿可学的 17 门覆盖、男性专属科目没有炫耀口上，事件前提按方案 §4.3 逐条比对与逐 token 校验，萝莉 1 / 20 的正文（Plan 32 起为期末 17 / 18），校验工具的新规则（期末桶禁 9 / 23、并入未成年与宿舍规则、成年桶只收显式推入的 uid），实操课「老学生 / 第一次来」读累计实操课次数、翘课 1004 读累计翘课数，检查成绩单档位 3 的四条口上与 1004 / 1005 排除档位 3，授课 / 听课占位行已删，ArkEditor 前提表同步与 buildata 生成器（ast 取出纯函数执行），`TEXT_DUP_LEN` 的 docstring 归位；Plan 32：事件表的前提与正文（M2 通用 3、L22 萝莉 1 / 20 / 26 挪进期末 17 / 18 / 19、L23 通用 6 / 15 / 27、L26 课型前提），实操课与检查成绩单的口上（L24 女儿前提、L25 成年女儿档位 3），target 行说明（L27）；Plan 32 实施复审补：`self_have_public_course` 的编辑器前提行、效果表 512 / 557 的说明、`CHILD_GROWTH` 的类说明 |
| `test_save_compat.py` | `CHILD_GROWTH` 字段级回填（含 Plan 30 的 `skip_count` / `skip_caught_day` / `semester_base_work_type`、Plan 31 的 `skip_class_day` / `sex_class_count` 补默认值）与成绩单迁移、教育区娱乐编号 175~178→152~155 的读档迁移（模板 / 单孩覆盖 / 娱乐槽位）、`save/` 下全部真实存档只读载入后的结构、学期结算幂等、四个面板可画；Plan 32：L22 旧履历的萝莉 1 / 20 / 26 读档改名为期末 17 / 18 / 19（孩子与罗德岛两处、幂等、新 uid 已有记录不覆盖）；本机没有 `save/` 或没有存档时跳过真实存档一段；Plan 32 实施复审补：迁移前后的残留检查，真实存档载入后没有萝莉 1 / 20 / 26 的残留 |
| `test_behavior_loop.py` | 模式 B：在女儿最多的真实存档上排课，从 08:40（到岗时间）起带护栏跑六轮行为循环，检查点落在每节课中段（节次交界处人人都在换教室）：收敛、学生岗的女儿进教室（课表只对学生岗生效，存档里改了岗的女儿不计入）、本节有课的教师在课表上那间教室授课、上午四节每人平均 ≥ 3 节出勤；再预约一节下午的实操课，断言开课时必修生都已离开上一节的教室、且已有人在实操教室等（路上撞上需求会晚到，所以不要求全员到场）；再跑一次跨天结算；Plan 32：L4 学生截短排在实时结算之前、截掉的那一段只算一遍（真实存档、真实行为循环）；本机没有 `save/` 或没有可读存档时整份跳过（PASS=0，行为循环的验收改看 `test_class_ai` 的循环段） |

## fixture 陷阱（本套件新增的，通用的见 `.github/skills/headless-game-test/SKILL.md`）

- `from _bootstrap import *` 默认不导出下划线开头的名字，翻译函数 `_` 靠 `_bootstrap.__all__` 显式导出。
- `behavior.start_time` 默认是公元 1 年，`get_class_period` 读的就是它——改游戏时间一律用 `set_time()`，它会同步全部角色。
- 实行值计算会读 `assistant_services[8]`，fixture 要 `get_assistant_services_zero()`。
- `TitleLineDraw / LittleTitleLineDraw` 的文字在 `title` 属性上，不在 `text`。
- 容器 `Education_Manage_Panel.draw()` 每轮会调 `basement.get_base_updata()`，最小 fixture 要把它钉死。
- 好感链的实际数值受信物 / 难度 / 连续指令影响，断言「走了好感结算」要打桩 `common_default.base_chara_favorability_and_trust_common_settle` 记录调用。
- 真实存档里总有几个没事可做的干员在「空闲 1 分钟」里一分钟一分钟地挪，45 分钟一轮要挪 45 步，护栏上限要 ≥ 60 才不会把它误判成卡死。
- 二段效果注册表是 `constant.settle_second_behavior_effect_data[效果id](角色id, change_data)`，与一段的四参数签名不同。
- 要直接跑 `handle_npc_ai.find_character_target`（`test_class_ai.py` 的 `dispatch`）：合成角色得先把 `action_info.wake_time` 设成当天（否则被「起床」目标 205 接管）、`clothing.get_npc_cloth` 穿上衣服（全裸时 `normal_all` 不成立，工作 / 娱乐的自动 AI 根本不跑）、`handle_premise.refresh_unnormal_flag`；每次派发前清掉 `behavior.move_target / move_final_target` 并把行为置回 `SHARE_BLANKLY`，否则被「继续移动」目标接管。育儿室的场景路径是 `["教", "育儿"]`（`SCENE_NURSERY`）。
- 用 Claude Code 的 Bash 工具往测试或代码里写含反斜杠转义（如双引号里的「反斜杠 + n」）的字符串时，heredoc 里的「反斜杠 + n」会被外层的 `eval` 包装压成真换行，写出来就是语法错误；含反斜杠的行改用 `chr(92)` 拼、或用 Write / Edit 工具写（Plan 29 踩过；本条原先的示例就被这个坑写坏了，Plan 32 改用文字描述）。
- 循环变量别用 `_`：它是 `_bootstrap` 导出的翻译函数，`for _ in range(...)` 之后同一文件里再调 `_()` 会报 `'int' object is not callable`（Plan 27 踩过）。
- 个人课表指向的教室课要在全局课表上排格子（`set_class_cell`）：空格子按「已停课」算没课，只排个人课表的夹具测不到上课（Plan 27）。同学关系与 `self_have_any_course` 也一样（Plan 30），只排个人课表的两个孩子不再互为同学。
- 这一节记了缺课就不再计出勤（Plan 30）：同一天同一节里先验了 721 / `settle_absent`、后面还要验出勤的，先清 `child_growth.last_absent_period`（`test_class_ai.py` 两道闸那一段的写法）。
- 按 cid 查 `game_config.config_target` / `config_target_premise_data` 时，键是「所在文件夹名 + cid」：`data/target/default/target.csv` 的 505 是 `"default505"`（`buildconfig.py` 的 `path_list[-2] + row[k]`），直接用 `"505"` 查会静默落空；列举一组行用 `config_target_type_index`。
- 婴儿不在 `npc_id_got`：`make_character(stage=101)` 与真实婴儿一致，不进这个集合（Plan 28；真实婴儿要到长成幼女才上线），要遍历孩子时别只看它。此前夹具把婴儿也放进去，婴儿期事件派不出来的问题一直没测出来。要造真实的婴儿用 `character_handle.born_new_character(母亲id, 名字)`，随后自己补 `pregnancy.born_time`（`test_growth_event.py` 的写法）。
- 跑整条 AI 派发前先看夹具穿没穿上衣服：`clothing.get_npc_cloth` 按 cid 查角色模板，部分 cid（Plan 31 复现踩到的 111、211、221、1101 等）一件都穿不上，`normal_4` 不成立，工作链与娱乐链的目标行全不跑，NPC 只会原地待机 5 / 10 分钟。用 101 / 201 这类有模板的 cid，或把它们的 `cloth` 深拷过去再 `refresh_unnormal_flag`（Plan 31）。
- 跑行为循环（`test_behavior_loop.run_one_round` 那种）时把夹具的体力 / 气力设成 2000：上限 100 时上一节课就跌到 1，下一节挂上疲劳去睡觉（状态机 78），`sp_flag.sleep` 还会让 303 拉不动；长循环里清了需求要 `refresh_unnormal_flag`，否则 `normal_1` 按旧掩码判不成立。真实的 `update_new_day` 在夹具里会卡在基建日结（缺 `materials_resouce`），要跑跨天时把 `basement.update_base_resouce_newday` 临时换成空函数（它排在角色刷新段之后，Plan 31）；另外夹具的 `cache.rhodes_island.party_day_of_week` 是空字典，`get_chara_entertainment` 按星期取派对日会 KeyError，先补成 `{0~6: 0}`（Plan 32 复现踩到）。
- 夹具的出生日要落在季月（3 / 6 / 9 / 12）：游戏时钟跳过非季月，阶段进度与「本阶段第几天」按可游玩天算（Plan 32），`born_time = now − N 天` 落在非季月时算出来远小于 N（7/19 出生到 9/7 只有 6 个可游玩天）。要造「阶段进度 X%」的孩子，从一个季月里的出生日起按真实时钟推算。
- 造「今天已翘课」的夹具要同时写 `skip_class_flag = True` 与 `skip_class_day = 当天的 toordinal()`（549 现在两样一起写）：只挂 flag 读作前一天的残留、不算今天翘课，翘课日的新路径一条都走不到（Plan 31 两份复现脚本改后核对时踩到）。
- 按 cid 查 `game_config.config_talk` / `config_talk_premise_data` 时，口上的 cid 是「上级目录名_文件名 + cid」：`data/talk/daily/check_report_card.csv` 的 1014 是 `"daily_check_report_card1014"`（`buildconfig.build_csv_config`），只写文件名会静默落空。也别按前提串去找口上：前提一改就一条都找不到，`all([])` 恒真（Plan 31）。
- 真实的成长结算（`pregnancy_handle._settle_baby_grow_up` / `check_grow_to_loli` / `check_grow_to_girl`）会调 `talk.must_show_talk_check` 画必显二段的口上，夹具里打桩成空函数、跑完还原；`_settle_baby_grow_up` 还读 `cache.rhodes_island.all_work_npc_set[教师岗]`、按幼女默认池重刷当天娱乐（Plan 32 L19），先 `all_work_npc_set.setdefault(E.TEACHER_WORK_TYPE, set())`、`open_all_classroom()`、补派对日表（`test_prenatal_baby.py` 的写法）。
- 学生赶去上课的截短（Plan 32 L4）已从 `judge_interrupt_character_behavior` 挪到 `handle_npc_ai.judge_student_leave_truncate`，由 `character_behavior` 的 NPC 分支在实时结算之前调：断言截出来的时长直接调它；探针只包 `judge_interrupt_character_behavior` 的会漏掉截短（`headless-game-test` 的包装已补）。
- 自建的行为循环（不经 `run_one_round`）要把参与的 NPC 的交互对象设回自己：`judge_character_status_time_over` 按「交互对象不在场」把行为的结束改写为这一步的结束，时间线会整段跳过（`test_behavior_loop.py` 的 L4 段）。
- 读档（`save_handle.input_load_save`）会重建全部角色对象：`_bootstrap` 导出的 `pl` 已不是存档里的玩家，读档后一律用 `cache.character_data[0]`（`run_one_round` 仍用夹具 `pl` 是旧问题，列在 Plan 32 方案 §7）。
- 跑真实的开课效果（383 的 10010 / 10014）时，10010 会调成就结算、读 `materials_resouce[1]`，夹具里要把 `achievement_panel.achievement_flow` 桩成空函数；课上玩家的 H 动作（含 6021 下课）走 `settle_behavior` 的群交分支、读 `confinement_training_setting[12]`，先 `attr_calculation.get_confinement_training_setting_zero()`（Plan 32 重建复现脚本时踩到）。
- 群交模式开着时跑行为循环（幽灵课堂那种），玩家体力一旦 ≤ 1，疲劳判定会走「群交中玩家体力归零」、经 `handle_group_sex_end` 调 `update.game_update_flow`，嵌套一层真实主循环后崩在成就结算缺 `materials_resouce`：`test_class_ai` 前面各段会把玩家体力耗到 1，这类段落要把玩家体力 / 气力设成 2000，并把 `game_update_flow` 换成记录桩（Plan 32 收尾补测试时踩到）。
- `test_settle_effects` 开头把 `character_behavior.judge_character_status` 全局桩成了空函数，原函数存在 `_real_judge_character_status`，要走真实的听课结算时临时换回来（Plan 32）。
- 带 `Weight|0` 的 CVP 不能交给 `handle_premise.handle_premise` 直接求值：`handle_comprehensive_value_premise` 没有 Weight 分支，会抛 `UnboundLocalError`（口上的 `get_weight_from_premise_dict` 在它之前就把这类前提截走了）；`high_` 前提已注册、返回的是权重值，要证明它被跳过只能用记录桩（Plan 32）。
- 真实存档不一定有养成数据与事件履历（2026-09-15 本机的 `save/0`、`save/11` 就没有）：读档迁移类断言在真实存档上只能验「没有残留」，改名本身靠构造的履历验（`test_save_compat` 的写法）。
- `Scene.character_list` 是 set，迭代顺序按哈希槽位而不是 cid（`{0, 190, 201}` 迭代成 0、201、190）：从场景名单里取人当玩家的交互对象、或按名单顺序断言时，别假设按 cid 排序，取完断言交互对象确是她（Plan 32 重建复现脚本时踩到）。
- 用真实循环让孩子翘课、挂上 flag 时，玩家别和她在同一个场景：她自己结算时就会当场派 623（翘课被抓），flag 清掉、记下被抓日期（Plan 32 重建复现脚本时踩到）。
- `test_behavior_loop` 与 `test_save_compat` 的真实存档段要仓库根目录的 `save/` 里有存档。存档不入库，换机器、换目录后没有时这两处打印「跳过」、不计失败（2026-09-15 起）；先判目录再调 `save_handle.judge_save_file_exist`，它经 `get_save_dir_path` 会顺手建出 `save` 目录。
