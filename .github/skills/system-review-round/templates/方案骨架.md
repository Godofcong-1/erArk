# Plan NN（方案）：<系统>第N轮复查

> 本 Plan 拆分为两个文件：**本文件为纯方案**（发现、现状调查、设计决策、接口设计、风险、范围外）；
> 逐文件改动步骤、构建、验证清单、回滚与实施过程记录见 `plan_NN_<系统>第N轮复查_实施步骤与记录.md`（下文简称"实施文档"）。
>
> 本 Plan 是 <主 Plan>、<既往各轮 Plan> 之后的又一轮全面复查，前述各 Plan 均在 `plan/done/`。
> 实施完成后在 <主 Plan / 总纲> 追加 §<N>，只写一行指向本 Plan。

- 状态：未实施（方案定稿 YYYY-MM-DD）
- 来源：用户需求 → "<用户原话>"
- 口径（待用户确认）：
  1. **<口径一句话>**：……（§3.1）
  2. ……
     - 附问 Q1：……（推荐）；备选……
- 预计改动量：**约 N 个文件**，净增约 X 行
  - 逻辑 N 个：……
  - 数据 N 个：……；工具 N 个：……
  - 测试约 N 个（含 README）；文档 N 个（说明文档、索引文档、主 Plan、`update.log`）
- 风险等级：<低 / 中低 / 中>。<一句理由：本轮唯一的数值口径变化是……>
- 适用代码快照：`master @ <commit>`（<上一轮 Plan> 提交之后）
- 前置：<既往各轮> 均已完成

---

## 1. 目标

1. <每条对应一条或一组发现，可独立验收>

**验收标准**：
- 全套回归测试通过，新增断言覆盖 §3 每一条
- 复现脚本（实施文档 §2.0）改前 N 项全部成立；改后 N 项问题命中不再成立，其余 N 项照旧
- <校验工具> 通过；<行为循环测试> 收敛、<关键指标> 不下降

## 2. 现状调查

本轮读过的范围：
- **`<模块目录>/` 全部 N 个模块**：逐行重读，含 <上一轮 Plan> 新加的函数
- **外部挂接点**：
  - `<文件>`：<编号 / 函数>
- **数据**：<CSV / 口上 / 事件>

基线：
- 回归 N 个文件 N 条断言全绿（YYYY-MM-DD，<上一轮 Plan> 之后）
- 死代码扫描：<模块目录> N 个定义，生产代码零引用：<注册式 N 个 / 其它>

下表的「已复现」都由 scratchpad 脚本在无头环境跑出（不入库，实施文档 §2.0 要求重建）。改前 N 项检查全部成立：N 项问题命中，N 项前提、对照与数据事实，另 N 项随口径。

### 2.1 发现（高 / 中）

| # | 发现 | 证据 | 状态 |
| --- | --- | --- | --- |
| M1 | **<一句话结论>**。<br>• <机理><br>• <影响> | `<file.py:行号>`；`<file.py:行号>` | 已复现：<br>• <关键实测值> |

### 2.2 发现（低）

| # | 发现 | 证据 | 状态 |
| --- | --- | --- | --- |
| L1 | …… | …… | …… |

**待定 Q1**：<问题与背景>
**回答**：<用户原话，拍板后填>

### 2.3 排查过、没有发现问题的疑点

留给下一轮复查参考，免得重复排查：

| 疑点 | 结论 |
| --- | --- |
| …… | …… |

### 2.4 可复用的既有实现

| 需求 | 现成实现 |
| --- | --- |
| …… | `<函数>`（<出处>） |

### 2.5 已知陷阱与硬约束

1. **<约束>**（<出处>）：……

## 3. 设计决策

### 3.1 <结论>（M1，推荐口径 1）

- <做法>

| 候选 | 弃选原因 |
| --- | --- |
| …… | …… |

### 3.N 小修（L…）

### 3.N 文档

- 说明文档：§<N>：……；维护注意事项新增一条「……」；测试计数
- 索引文档：……
- 主 Plan / 总纲：追加 §<N>，一行指向本 Plan

## 4. 接口设计（实施的权威定义）

### 4.1 常量与字段

```python
<新常量 / 新字段，附中文 docstring>
```

### 4.2 函数签名

| 模块 | 函数 | 变化 |
| --- | --- | --- |
| `<模块>` | `<签名>` | <新增 / 改动>（§3.N） |

### 4.3 数据行

| 文件 | 行 | 改为 |
| --- | --- | --- |
| `<CSV>` | cid N | …… |

## 5. 行为对照

| 情形 | 现行为 | 改后 |
| --- | --- | --- |
| …… | …… | …… |

## 6. 风险与注意事项

| 风险 | 说明 | 对策 |
| --- | --- | --- |
| 新字段 | 存档结构变化 | 属性表整体回填；`test_save_compat` 断言 |
| 构建会写乱 PO | 本机无 gettext | 收尾 `git checkout -- data/po/` |

## 7. 不在本方案范围

- <明确排除的相邻问题，附出处；下一轮的已知清单从这里取>

## 8. 追加调整

（实施后每轮追加一节，只写设计；实施记录见实施文档 §6.5。暂无）
